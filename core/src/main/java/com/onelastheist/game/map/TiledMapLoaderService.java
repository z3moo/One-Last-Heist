package com.onelastheist.game.map;

/** Lop bien cho viec tai file .tmx trong tuong lai. */
public class TiledMapLoaderService {
    public HouseMap loadHouseMap(String mapPath) { return new HouseMap(); }
}
